# Build outputs and dependencies are produced inside the image, never copied in.
web/node_modules
web/dist
server/playground

# Nothing here affects the build; excluding it keeps the context small and
# stops a local .env from ever landing in a layer.
.git
.github
.gitignore
*.md
docker-compose*.yml
.env
.env.*
